<!-- etstsr.svg -->
<svg xmlns="http://www.w3.org/2000/svg" width="200" height="28" viewBox="0 0 200 28" role="img" aria-labelledby="title desc">
  <title id="title">ETSTSR</title>
  <desc id="desc">Altın arkaplanlı proje rozeti</desc>

  <defs>
    <!-- Altın degradeyi tanımla -->
    <linearGradient id="gold" x1="0" y1="0" x2="0" y2="1">
      <stop offset="0%" stop-color="#FFE47A"/>
      <stop offset="100%" stop-color="#FFD700"/>
    </linearGradient>

    <!-- Hafif gölge -->
    <filter id="shadow" x="-50%" y="-50%" width="200%" height="200%">
      <feDropShadow dx="0" dy="1" stdDeviation="0.6" flood-opacity="0.2"/>
    </filter>

    <!-- Metin stili: SVG içi CSS güvenli ve çalışır -->
    <style><![CDATA[
      .badge {
        font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, "Apple Color Emoji","Segoe UI Emoji";
        font-weight: 600;
        font-size: 13px;
        fill: #111; /* altın üzerinde koyu metin */
      }
    ]]></style>
  </defs>

  <!-- Arkaplan -->
  <rect rx="6" width="200" height="28" fill="url(#gold)" filter="url(#shadow)"/>

  <!-- Metin -->
  <text x="50%" y="50%" class="badge" dominant-baseline="middle" text-anchor="middle">ETSTSR 🛠</text>
</svg>
